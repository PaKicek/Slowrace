func int factorial (int n) { // up to 20!
    int result = 1;
    for (int i = 2; i <= n; i++) {
        result = result * i;
    }
    return result;
}

main (int argc, array string argv[]) {
    if (argc >= 2) {
        int n = to_int(argv[1]);
        int result = factorial(n);
        print("Factorial of the number is ");
        println(result);
    } else {
        println("No number is passed");
    }
}